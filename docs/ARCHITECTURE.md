# ARCHITECTURE

Frontend: React + Vite + Tailwind. Backend: Clean Architecture (.NET 9,
Dapper, JWT). Flow: Controller -\> Service -\> Repository -\> SQL
Server. Use dependency injection and reusable components.
